# Security and privacy

Do not commit runtime state or credentials. This repository intentionally ignores app/dependencies.json, data, jobs, logs, browser profiles, bridge files, backups and local status exports.

The accepted Windows scope is browser/proxy routing. The current release is not a validated full-system VPN, kill switch, or leak-proof network boundary.
