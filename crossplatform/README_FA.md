# FreeNet Hub 4.1.2 - Cross-platform
Windows is runtime-accepted for browser/proxy scope. Linux source is locally static-tested on WSL when available. Android/iOS are native build packs that fail closed until a real forwarding core is integrated; they are not falsely labeled as runtime VPN builds.
