# FreeNet Hub private obfs4 bridge file template.
# Do not commit real bridge lines.
# Add private bridge lines only to:
#   ~/.local/share/FreeNetHub/bridges_obfs4.txt
# Expected form:
#   obfs4 <host>:<port> <fingerprint> cert=<certificate> iat-mode=0
