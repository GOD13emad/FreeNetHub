# FreeNet Hub obfs4 bridge template.
# Runtime/private bridge lines are intentionally not packaged.
# Import valid complete bridge lines through the UI or place them in:
# ~/.local/share/FreeNetHub/bridges_obfs4.txt
